{
  "name": "@stockos/web",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm install --prefix ../api && npm install --prefix ../worker && npm --prefix ../api run typecheck && npm --prefix ../worker run typecheck && tsc --noEmit && next build",
    "start": "next start"
  },
  "dependencies": {
    "@coinbase/cdp-core": "0.0.123",
    "@coinbase/cdp-hooks": "0.0.123",
    "@coinbase/cdp-react": "0.0.123",
    "next": "16.3.4",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
